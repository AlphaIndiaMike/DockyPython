apiVersion: 1

datasources:
  - name: MongoDB
    type: grafana-mongodb-datasource
    url: http://mongo:27017
    database: ticker_database
    jsonData:
      timeField: _id  # Change this to the appropriate time field in your data
    editable: false
